// package: schedule.js

// The code consists of two parts.
//
// - Global scripts without import/export. These are ordered according
//   to the /// <reference path="..." /> pragmas. All script files are
//   included, regardless of whether they are referenced via pragmas.
// - Module scripts with import/export. These are ordered according
//   to the import statements. Only modules reachable via this index.ts
//   entry point are included.
//
// Scripts are included after the modules. Any modifications to the global
// scope performed by the modules will be visible to the scripts.

import "./FullCalendar.js";
